hello,
i have a project that i want to build. so i'm a Yeastar solution provider, and i mainely install for our client Yeastar P-series software edition and it has AI from Yeastar, but some client want to use different AI provider. now i have tested a solution and it connect ai agent from openai or elevenlabs or gemini, but i want to build the same for other ai agent and it should need just api credential.
here is SIP4AI https://sip4ai.com/docs/api and here is Yeastar https://help.yeastar.com/en/p-series-software-edition/administrator-guide/about-this-guide.html
voila exemple dial fichier service dial SIP4AI pour la connexion avec l’extension Yeastar 1005
[Unit] Description=SIP4AI After=network.target
[Service] WorkingDirectory=/opt/sip4ai Environment=CONFIG_FILE=config.json Environment=SIP_USERNAME=1005 Environment=SIP_AUTH_ID=TjoCu4uhmD Environment=SIP_PASSWORD=EmkWA6lT7y Environment=SIP_DOMAIN=105.188.31.241 Environment=SIP_SERVER=105.188.31.241:5060 Environment=ELEVEN_LABS_API_KEY=sk_4c03d6200fe7f5330d48e4e53c1d52f3c4f9f5b87b79da05 ExecStart=/usr/local/bin/sip4ai Restart=always RestartSec=5
[Install] WantedBy=multi-user.target
(105.188.31.241) ip public dial installation yeastar (1005) heya Extension Number (TjoCu4uhmD) howa Registration Name (EmkWA6lT7y) howa Registration Password
what do you think?

and i have asked kimi and he told me that we can also make a front end to see registred extension like status of our system

here is his response

Salam! I understand exactly what you're building — a flexible **AI voice agent gateway** for your Yeastar clients where switching AI providers is as simple as dropping in API credentials. Your architecture diagram is spot on.
Here's what I see from the SIP4AI docs — they already natively support **5 AI providers**:
- **OpenAI** (Realtime API)
- **ElevenLabs** (Conversational AI)
- **Google Gemini** (Live API)
- **Deepgram** (Voice Agent)
- **Cartesia** (Sonic)
The switching mechanism is already there via `config.json` → `"provider"` field. What you're really building is a **multi-client management layer** on top of SIP4AI, so you don't have to manually SSH into servers and edit systemd files for every client.
---
## What I Recommend Building
A **SIP4AI Configuration Manager** — a lightweight web dashboard where you can:
| Feature | Description |
|--------|-------------|
| **Extension Mapping** | Add Yeastar extensions (1005, 1006, etc.) with their SIP credentials |
| **AI Provider Selector** | Pick OpenAI/ElevenLabs/Gemini/etc. per extension |
| **Credential Vault** | Store API keys securely, inject them into services |
| **Config Generator** | Auto-generate `config.json` + systemd service files |
| **Deploy & Monitor** | Start/stop services, see SIP registration status |
This lets you onboard a new client in **2 minutes** instead of manual server config.
---
## Architecture I Propose
```
┌─────────────────────────────────────────┐
│      SIP4AI Config Manager (Web UI)     │
│  ┌─────────┐ ┌─────────┐ ┌───────────┐ │
│  │Extension│ │  AI     │ │  Service  │ │
│  │ Manager │ │Provider │ │  Control  │ │
│  └────┬────┘ └────┬────┘ └─────┬─────┘ │
│       └───────────┴────────────┘        │
└──────────────────┬──────────────────────┘
                   │ generates
        ┌──────────┴──────────┐
        ▼                     ▼
   config.json           systemd services
        │                     │
        └──────────┬──────────┘
                   ▼
            ┌─────────────┐
            │  SIP4AI     │  ←→  Yeastar PBX
            │  Instances  │  ←→  AI Provider APIs
            └─────────────┘
```
---
## Let Me Build This For You
I can build a **working prototype** right now — a React web app with:
1. **Client/extension management** form
2. **AI provider selector** with per-provider config fields
3. **Config file generator** (download `config.json` + `.service` file)
4. **Docker Compose setup** for easy deployment
**Want me to build this?** It'll be a single-page app you can run locally, generate configs, and eventually deploy on a small VPS next to your SIP4AI instances.
Just say **"Yes, build it"** and I'll start coding the full working prototype. I can have the first version ready in a few minutes with:
- Extension CRUD
- Provider switcher (OpenAI / ElevenLabs / Gemini / Deepgram / Cartesia)
- Auto-generated systemd service files
- Downloadable `config.json` per extension